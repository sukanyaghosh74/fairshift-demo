# FairShift - AI Shift Manager Demo

## Run Instructions (2 commands):

Backend:
`ash
cd backend
pip install -r requirements.txt
uvicorn app:app --reload
`

Frontend:
`ash
cd frontend
npm install
npm run dev
`
